var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Loss-functions","page":"API","text":"","title":"Loss functions"},{"category":"section","location":"api/#Helpers","page":"API","text":"","title":"Helpers"},{"category":"section","location":"api/#Internal-(advanced)","page":"API","text":"","title":"Internal (advanced)"},{"category":"section","location":"api/#Summary","page":"API","text":"Function Description\nrnnt_loss_batched Batched RNN-T / vanilla transducer loss (Graves 2012)\ntdt_loss_batched Batched Token-and-Duration Transducer loss (Xu et al., ICML 2023)\nmonotonic_rnnt_loss_batched Monotonic RNN-T via TDT with durations = [1]\npack_transducer_targets Pack ragged label sequences into a padded matrix\nrnnt_forward_backward Internal RNN-T forward-backward; returns (loss, grad)\ntdt_forward_backward Internal TDT forward-backward; returns (loss, grad_tok, grad_dur)","title":"Summary"},{"category":"module","location":"api/#TransducerLoss","page":"API","text":"TransducerLoss\n\nThe transducer-loss family for Julia — GPU-capable via KernelAbstractions, with analytic gradients delivered through ChainRulesCore rrules so Zygote never traces the lattice kernels. The companion package to CTCLoss.jl, sharing its conventions: blank defaults to the last class, the loss is the batch mean, and gradients w.r.t. raw logits are computed inside the forward-backward.\n\nTwo losses, one lattice core:\n\nrnnt_loss_batched — vanilla RNN-T (Graves 2012, arXiv:1211.3711): blank advances time by one, labels advance the label axis in place.\ntdt_loss_batched — Token-and-Duration Transducer (Xu et al., ICML 2023, arXiv:2304.06795): a second duration head lets every emission advance time by a learned stride, enabling frame-skipping decode; with durations = [0, 1] its alignment space strictly contains RNN-T's.\nmonotonic_rnnt_loss_batched — Monotonic RNN-T as TDT with durations = [1] (diagonal-only alignments).\nFastEmit — fastemit_lambda on RNN-T and TDT (Yu et al., arXiv:2010.11148); gradient-only latency regularization.\n\nThey live in one package deliberately: both operate on the same (t frames, u labels) lattice and share the emission gather, α initialization, NLL read-out, label packing, numerics, and test oracles (src/core/). The src/rnnt/ and src/tdt/ subtrees depend only on core/, never on each other — so splitting one out later is trivial, but bundling avoids duplicated kernels or a micro-package depending on another package's internals (the same reason NeMo, k2, and torchaudio ship their transducer variants together).\n\nBoth forward-backwards run as diagonal wavefronts: all lattice cells with t + u = d are independent given neighbouring diagonals, so the host loops over diagonals and each kernel launch fills one anti-diagonal for the whole batch. Every recurrence and gradient formula was validated against brute-force enumeration of all lattice paths and central finite differences (max error ~1e-10) before transcription; the test suite repeats both checks in pure Julia.\n\n\n\n\n\n","title":"TransducerLoss"},{"category":"function","location":"api/#TransducerLoss.rnnt_loss_batched","page":"API","text":"rnnt_loss_batched(logits, targets, input_lengths [; blank, fastemit_lambda])\nrnnt_loss_batched(logits, targets, input_lengths, blank [; fastemit_lambda])\n\nBatched RNN-T / transducer loss (Graves 2012), mean over batch.\n\nlogits: (V, T, U+1, B) raw joint-network outputs (softmax applied internally), where T bounds encoder frames and U+1 bounds prediction states (⟨start⟩ plus up to U labels).\ntargets: Vector{Vector{Int}} label sequences (no blank).\ninput_lengths: valid encoder frames per sample.\nblank defaults to size(logits, 1) (last class), matching CTCLoss.jl.\nfastemit_lambda: FastEmit regularization (Yu et al., arXiv:2010.11148). Scales label-emission gradients by (1 + λ); 0 disables. The reported loss scalar is unchanged (standard NLL); only gradients are modified, matching torchaudio / NeMo / k2.\n\nGradients are supplied through a ChainRulesCore rrule computed inside the same forward-backward pass, so Zygote never traces the kernels. Memory note: the joint tensor is O(V·T·U·B) — for long inputs train on shorter windows or smaller batches.\n\n\n\n\n\n","title":"TransducerLoss.rnnt_loss_batched"},{"category":"function","location":"api/#TransducerLoss.tdt_loss_batched","page":"API","text":"tdt_loss_batched(logits, dur_logits, targets, input_lengths, durations;\n                 blank = size(logits, 1), sigma = 0, fastemit_lambda = 0)\n\nBatched Token-and-Duration Transducer loss (Xu et al., ICML 2023, arXiv:2304.06795) — the frame-skipping generalization of RNN-T behind NVIDIA's Parakeet-TDT models. Mean over the batch; blank defaults to the last token class (the CTCLoss.jl / TransducerLoss.jl convention).\n\nlogits: (V, T, U+1, B) raw token-head outputs.\ndur_logits: (D, T, U+1, B) raw duration-head outputs, one class per entry of durations.\ndurations: ascending duration set, e.g. [0, 1, 2, 3, 4]. Blank transitions use durations ≥ 1; tokens may use 0 (multiple tokens per frame, as in RNN-T). The exit blank lands exactly on frame T + 1.\nsigma: NeMo's logit under-normalization constant (≈ 0.05 recommended; 0 disables).\nfastemit_lambda: FastEmit regularization; scales label-emission gradients by (1 + λ) on both token and duration heads. Loss scalar unchanged; see rnnt_loss_batched.\n\nWith durations = [0, 1] the alignment space is a strict superset of vanilla RNN-T's (tokens may also advance time directly). With durations = [1] every emission advances time by one frame — equivalent to Monotonic RNN-T (see monotonic_rnnt_loss_batched). Gradients for both logit tensors arrive through a ChainRulesCore rrule computed inside the same forward-backward pass.\n\n\n\n\n\n","title":"TransducerLoss.tdt_loss_batched"},{"category":"function","location":"api/#TransducerLoss.monotonic_rnnt_loss_batched","page":"API","text":"monotonic_rnnt_loss_batched(logits, targets, input_lengths;\n                            blank = size(logits, 1), sigma = 0,\n                            fastemit_lambda = 0)\n\nBatched Monotonic RNN-T loss — at most one label per frame, no vertical token transitions. Implemented as TDT with durations = [1] and a trivial duration head (dur_logits all zeros ⇒ softmax probability 1). Equivalent to restricting the lattice to diagonal label moves plus blank advances.\n\n\n\n\n\n","title":"TransducerLoss.monotonic_rnnt_loss_batched"},{"category":"function","location":"api/#TransducerLoss.pack_transducer_targets","page":"API","text":"pack_transducer_targets(targets, blank) → (labels, target_lengths)\n\nPack ragged label sequences into a zero-padded (Umax, B) Int32 matrix plus Vector{Int32} lengths. Targets must not contain blank (the lattice reserves it for the advance-time transition) — throws ArgumentError if they do.\n\n\n\n\n\n","title":"TransducerLoss.pack_transducer_targets"},{"category":"function","location":"api/#TransducerLoss.rnnt_forward_backward","page":"API","text":"rnnt_forward_backward(logits, labels, target_lengths, input_lengths,\n                      blank, fastemit_lambda)\n\nInternal: run the transducer forward-backward on whatever device holds logits and return (loss, grad) where loss is the batch-mean NLL and grad is ∂loss/∂logits (same shape as logits, already scaled by 1/B). Use pack_transducer_targets to build labels and target_lengths.\n\n\n\n\n\n","title":"TransducerLoss.rnnt_forward_backward"},{"category":"function","location":"api/#TransducerLoss.tdt_forward_backward","page":"API","text":"tdt_forward_backward(logits, dur_logits, labels, target_lengths,\n                     input_lengths, durations, blank, sigma)\n\nInternal: run the TDT forward-backward on whatever device holds logits and return (loss, grad_tok, grad_dur) — the batch-mean NLL and the gradients w.r.t. both raw-logit tensors (already scaled by 1/B).\n\n\n\n\n\n","title":"TransducerLoss.tdt_forward_backward"},{"category":"function","location":"api/#TransducerLoss.logaddexp","page":"API","text":"logaddexp(a, b)\n\nNumerically stable log(exp(a) + exp(b)). Internal; qualify as TransducerLoss.logaddexp if needed.\n\n\n\n\n\n","title":"TransducerLoss.logaddexp"},{"category":"section","location":"#TransducerLoss.jl","page":"Home","text":"The transducer-loss family for Julia — the companion package to CTCLoss.jl, sharing its conventions (blank = last class by default, loss = batch mean, analytic gradients w.r.t. raw logits delivered through ChainRulesCore rrules, device-agnostic kernels via KernelAbstractions — CPU and CUDA run the same code).","title":"TransducerLoss.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url = \"https://github.com/mashu/TransducerLoss.jl\")\n\nFrom a local checkout:\n\nPkg.develop(path = \"/path/to/TransducerLoss.jl\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using TransducerLoss\n\n# Vanilla RNN-T (Graves 2012): logits (V, T, U+1, B)\nloss = rnnt_loss_batched(logits, targets, input_lengths)            # blank = V\nloss = rnnt_loss_batched(logits, targets, input_lengths, blank)\n\n# TDT — Token-and-Duration Transducer (Xu et al., ICML 2023):\n# token head (V, T, U+1, B) + duration head (D, T, U+1, B)\nloss = tdt_loss_batched(logits, dur_logits, targets, input_lengths,\n                        [0, 1, 2, 3, 4]; blank, sigma = 0.05)\n\n# Monotonic RNN-T (diagonal-only alignments): TDT with durations = [1]\nloss = monotonic_rnnt_loss_batched(logits, targets, input_lengths)\n\n# FastEmit latency regularization (torchaudio / NeMo / k2 compatible)\nloss = rnnt_loss_batched(logits, targets, input_lengths; fastemit_lambda = 0.004)","title":"Quick start"},{"category":"section","location":"#Tensor-shapes","page":"Home","text":"Logits: (V, T, U+1, B) — raw joint-network outputs before softmax. T bounds encoder frames; U+1 bounds prediction states (⟨start⟩ plus up to U labels).\nTargets: Vector{Vector{Int}} — label indices (no blank).\nInput lengths: Vector{Int} — valid encoder frames per sample.\nBlank defaults to size(logits, 1) (last class), matching CTCLoss.jl.\n\nFor TDT, dur_logits has shape (D, T, U+1, B) with one class per entry of durations (ascending, e.g. [0, 1, 2, 3, 4]). The sigma parameter is NeMo's logit under-normalization constant (≈ 0.05 recommended; 0 disables).","title":"Tensor shapes"},{"category":"section","location":"#Automatic-differentiation","page":"Home","text":"Gradients are supplied through ChainRulesCore rrules computed inside the forward-backward pass, so Zygote never traces the lattice kernels:\n\nusing TransducerLoss, Zygote\n\ngrad = Zygote.gradient(\n    l -> rnnt_loss_batched(l, targets, input_lengths),\n    logits,\n)[1]\n\ngtok, gdur = Zygote.gradient(\n    (l, d) -> tdt_loss_batched(l, d, targets, input_lengths, durations),\n    logits, dur_logits,\n)","title":"Automatic differentiation"},{"category":"section","location":"#Why-one-package-for-two-losses","page":"Home","text":"Both losses operate on the same (t frames consumed, u labels emitted) lattice, and TDT with durations = [0, 1] has an alignment space that strictly contains RNN-T's. They share the emission gather, α initialization, NLL read-out, label packing, numerics, and test oracles — all of which live in src/core/. The src/rnnt/ and src/tdt/ subtrees depend only on core/, never on each other, so extracting one into its own package later is a file move; bundling today avoids either duplicated kernels or a micro-package reaching into another package's unexported internals. NeMo, k2, and torchaudio ship their transducer variants together for the same reason.\n\nsrc/\n  TransducerLoss.jl   module root\n  core/\n    utils.jl          logaddexp\n    labels.jl         pack_transducer_targets\n    kernels.jl        emission gather, α init, NLL read-out (loss-agnostic)\n  rnnt/\n    kernels.jl        RNN-T diagonal + gradient kernels\n    loss.jl           driver, API, rrules\n  tdt/\n    kernels.jl        TDT diagonal + gradient kernels\n    loss.jl           driver, API, rrule\ntest/\n  reference.jl        pure-Julia references + brute-force oracles\n  test_rnnt.jl, test_tdt.jl","title":"Why one package for two losses"},{"category":"section","location":"#Algorithms","page":"Home","text":"Forward-backwards run as diagonal wavefronts: all lattice cells with t + u = d are independent given neighbouring diagonals — one kernel launch per diagonal, parallel over (u, batch).\n\nRNN-T: blank advances time by one, label y_{u+1} advances the label axis in place; exit via a final blank from (T, U+1).\n\nTDT (NeMo-compatible semantics): two independently normalized heads; every transition also draws a duration d from an ascending set — blank requires d ≥ 1 (no self-loop), tokens may take d = 0; the exit blank lands exactly on frame T + 1; all transition scores are under-normalized by a constant sigma (paper recommends ≈ 0.05, 0 disables). At decode time the duration head's prediction advances the encoder index — the frame-skipping behind the 2–3× throughput lead of TDT models on the open ASR leaderboards.\n\nFastEmit (Yu et al., arXiv:2010.11148): optional fastemit_lambda on both rnnt_loss_batched and tdt_loss_batched. Scales label-emission gradients by (1 + λ); the loss scalar stays the standard NLL (gradient-only regularization, as in torchaudio / NeMo / k2). Verified analytically against label-emission posteriors; λ = 0 recovers the base transducer.","title":"Algorithms"},{"category":"section","location":"#Transducer-family-coverage","page":"Home","text":"Variant Status in this package\nRNN-T (Graves 2012) rnnt_loss_batched\nTDT (Xu et al., ICML 2023) tdt_loss_batched\nMonotonic RNN-T monotonic_rnnt_loss_batched — TDT with durations = [1]\nFastEmit regularization fastemit_lambda kwarg on RNN-T and TDT\nMulti-blank transducer Subsumed by TDT (duration head generalizes frame skipping)\nRNA / diagonal-only Same lattice as Monotonic RNN-T (durations = [1])\nPruned RNN-T (k2) Planned — banded lattice, same objective, ~10× memory\nHAT (blank factorization + ILM) Planned — separate blank stream for shallow fusion\nBayes-risk / delay-penalty Out of scope for v0.1 (k2-style risk objectives)\nAlignment-restricted (Ar-RNN-T) Out of scope (requires external alignments)\n\nMonotonic RNN-T is not a separate kernel tree: every token transition must advance time by exactly one frame, so the alignment lattice is a subset of vanilla RNN-T's. Setting TDT durations = [1] enforces this — blank and label moves are both diagonal, with a trivial single-class duration head.","title":"Transducer family coverage"},{"category":"section","location":"#Verification","page":"Home","text":"Every recurrence and gradient formula was validated before transcription against brute-force enumeration of all lattice paths and central finite differences (max error ≈ 1e-10 in double precision), including empty targets, T = 1, zero-length inputs, padded prediction axes, duration sets without 0, and sigma ≠ 0. FastEmit (λ > 0) is checked by decomposing gradients into the λ = 0 base plus label-posterior scaling. The test suite repeats every check in pure Julia, plus Zygote integration (both heads for TDT) and heterogeneous-batch consistency.","title":"Verification"},{"category":"section","location":"#Memory","page":"Home","text":"Both losses materialize O(T · U · B) lattice tensors and consume O(V · T · U · B) joint logits — the classic transducer cost. Train on shorter windows or smaller batches for long inputs. The natural next optimizations: a gather-based fused joint, and the pruned transducer (k2), which bounds the lattice to a thin band for ~10× memory reduction at equal accuracy.","title":"Memory"},{"category":"section","location":"#Dependencies","page":"Home","text":"KernelAbstractions\nChainRulesCore\nNNlib (for logsoftmax)","title":"Dependencies"},{"category":"section","location":"#License","page":"Home","text":"MIT.","title":"License"}]
}
