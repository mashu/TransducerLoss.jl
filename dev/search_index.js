var documenterSearchIndex = {"docs":
[{"category":"section","location":"variants/#Choosing-a-loss","page":"Choosing a loss","text":"Pick a variant from the table below before reading API details. Every entry point is a loss function on logits your model already computed.","title":"Choosing a loss"},{"category":"section","location":"variants/#Decision-table","page":"Choosing a loss","text":"Variant Entry point Logit inputs (shapes) When to use Why it exists\nRNN-T rnnt_loss logits (V, T, U+1, B) Default transducer training; Graves-style blank/label lattice Baseline end-to-end ASR objective (Graves 2012)\nTDT tdt_loss logits (V, T, U+1, B) + dur_logits (D, T, U+1, B) + durations (AbstractVector or NTuple of ints) Frame-skipping decode, Parakeet-TDT-style models, multi-blank / variable stride Generalizes RNN-T with a duration head (Xu et al., ICML 2023)\nMonotonic RNN-T monotonic_rnnt_loss logits (V, T, U+1, B) At most one label per frame; RNA / diagonal-only alignments Restricts lattice to diagonal moves (TDT with durations = [1])\nHAT hat_loss label_logits (V, T, U+1, B) + blank_logits (T, U+1, B) — blank-free V LM fusion with internal-LM subtraction; production shallow-fusion setups Factorized blank enables principled LM combination (Variani et al., ICASSP 2020)\nPruned RNN-T pruned_rnnt_loss + pruning_bounds pruned_logits (V, T, S, B) + band_offsets (T, B); bounds helper takes am (V, T, B) + lm (V, U+1, B) Long utterances where full O(V·T·U·B) joint does not fit in memory Banded lattice matches vanilla RNN-T when band is wide enough (Kuang et al., Interspeech 2022)\nPruned TDT pruned_tdt_loss + tdt_pruning_bounds Token (V, T, S, B) + duration (D, T, S, B) + band_offsets (T, B); bounds helper also takes am_dur (D, T, B) + lm_dur (D, U+1, B) Same memory bottleneck as pruned RNN-T, but for duration-head models Banding for TDT; bounds use the TDT lattice (occupancy), not RNN-T's\nFastEmit fastemit_lambda on RNN-T / TDT / pruned TDT Same as parent variant Reduce emission latency at decode time Gradient-only regularization; loss scalar unchanged (Yu et al., arXiv:2010.11148)\nMinimum-latency latency_lambda on RNN-T only Same as RNN-T Penalize late label emissions during training Adds exact expected emission frame to the objective (Shinohara & Watanabe, Interspeech 2022)","title":"Decision table"},{"category":"section","location":"variants/#Tensor-shape-legend","page":"Choosing a loss","text":"Symbol Meaning\nV Vocabulary size (includes blank for RNN-T / TDT / pruned; excludes blank for HAT labels)\nT Max encoder frames (padded axis)\nU Max target length in the batch\nB Batch size\nD Number of duration classes (length(durations))\nS Band width (size(pruned_logits, 3)); lattice cell u = band_offsets[t, b] + s\n\nTDT extras: durations must be ascending, unique, non-negative, and include a value ≥ 1. Blank transitions use durations ≥ 1; tokens may use d = 0. sigma (default 0) is NeMo's logit under-normalization (≈ 0.05 recommended).\n\nPruned extras: band_offsets must be monotone per sample with band_offsets[1, b] == 0. pruning_bounds estimates offsets from trivial-joint posteriors (k2-inspired, not bit-compatible with k2's gradient-based bounds). tdt_pruning_bounds does the same on the TDT lattice, centring on cell occupancy rather than label-emission posterior — frame-skipping blanks move mass, so RNN-T bounds are the wrong estimator for TDT.","title":"Tensor shape legend"},{"category":"section","location":"variants/#Typical-use-cases","page":"Choosing a loss","text":"Start with RNN-T when building a standard encoder–predictor–joint transducer and memory is not a bottleneck. One logit tensor, well-understood training dynamics.\n\nPick TDT when the model has separate token and duration heads and you want frame skipping at decode (e.g. NVIDIA Parakeet-TDT). Use durations = [0, 1, 2, 3, 4] and sigma ≈ 0.05 for NeMo-compatible training. Subsumes multi-blank transducer variants.\n\nPick Monotonic RNN-T when alignments must stay on the diagonal (one token per frame maximum). No duration head in your model — pass only token logits.\n\nPick HAT when you need internal-LM subtraction for principled fusion with an external language model. Your joint network outputs separate label and blank logits over a blank-free label vocabulary.\n\nPick Pruned RNN-T when utterances are long and the full (V, T, U+1, B) joint tensor is too large. Use pruning_bounds to get band_offsets, gather logits into (V, T, S, B), then call pruned_rnnt_loss. With S = U + 1 and zero offsets the loss equals vanilla RNN-T exactly.\n\nPick Pruned TDT for the same memory reason when the model has a duration head. Use tdt_pruning_bounds (not RNN-T's helper), gather both token and duration logits onto the band, then call pruned_tdt_loss. Full-width band equals tdt_loss; FastEmit works on the banded path.\n\nAdd fastemit_lambda (RNN-T, TDT, or pruned TDT) when decode latency is too high — scales label-emission gradients by (1 + λ) without changing the reported NLL. Typical values are small (e.g. 0.004).\n\nAdd latency_lambda (RNN-T only) when you want the loss itself to penalize late emissions via the alignment posterior's expected frame index.","title":"Typical use-cases"},{"category":"section","location":"variants/#Out-of-scope","page":"Choosing a loss","text":"Idea Status\nMulti-blank transducer Subsumed by TDT duration head\nBayes-risk / delay-penalty (k2-style) Not implemented\nAlignment-restricted RNN-T (external alignments) Not implemented","title":"Out of scope"},{"category":"section","location":"variants/#Memory","page":"Choosing a loss","text":"Vanilla RNN-T, TDT, and HAT materialize O(T · U · B) lattice state and consume O(V · T · U · B) joint logits. Pruned RNN-T / TDT bound the joint to O(V · T · S · B) (plus O(D · T · S · B) duration logits for TDT) with S ≪ U. Shorter training windows or smaller batches help when memory is tight.","title":"Memory"},{"category":"section","location":"variants/#Automatic-differentiation","page":"Choosing a loss","text":"Gradients are analytic rrules computed inside the forward-backward — Zygote never traces the lattice kernels. See Examples for a Zygote snippet.","title":"Automatic differentiation"},{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Loss-functions","page":"API","text":"","title":"Loss functions"},{"category":"section","location":"api/#Helpers","page":"API","text":"","title":"Helpers"},{"category":"section","location":"api/#Internal-(advanced)","page":"API","text":"","title":"Internal (advanced)"},{"category":"section","location":"api/#Summary","page":"API","text":"Function Description\nrnnt_loss RNN-T / vanilla transducer loss (Graves 2012); (V,T,U+1,B) or (V,T,U+1)\ntdt_loss Token-and-Duration Transducer loss (Xu et al., ICML 2023); 4D or 3D\nmonotonic_rnnt_loss Monotonic RNN-T via TDT with durations = [1]\nhat_loss Hybrid Autoregressive Transducer with Bernoulli blank factorization\npruned_rnnt_loss RNN-T on a banded lattice; full-width band equals vanilla RNN-T\npruned_tdt_loss TDT on a banded lattice; full-width band equals vanilla TDT\npruning_bounds Estimate band offsets from trivial-joint label posteriors\ntdt_pruning_bounds Estimate band offsets on the TDT lattice (occupancy-centred)\npack_transducer_targets Pack ragged label sequences into a padded matrix\nrnnt_forward_backward Internal RNN-T forward-backward; returns (loss, grad)\ntdt_forward_backward Internal TDT forward-backward; returns (loss, grad_tok, grad_dur)\nhat_forward_backward Internal HAT forward-backward; returns (loss, grad_labels, grad_blank)\npruned_forward_backward Internal banded forward-backward; returns (loss, grad)","title":"Summary"},{"category":"module","location":"api/#TransducerLoss","page":"API","text":"TransducerLoss\n\nThe transducer-loss family for Julia — GPU-capable via KernelAbstractions, with analytic gradients delivered through ChainRulesCore rrules so Zygote never traces the lattice kernels. The companion package to CTCLoss.jl, sharing its conventions: blank defaults to the last class, the loss is the batch mean, and gradients w.r.t. raw logits are computed inside the forward-backward.\n\nTwo losses, one lattice core:\n\nrnnt_loss — vanilla RNN-T (Graves 2012, arXiv:1211.3711): blank advances time by one, labels advance the label axis in place.\ntdt_loss — Token-and-Duration Transducer (Xu et al., ICML 2023, arXiv:2304.06795): a second duration head lets every emission advance time by a learned stride, enabling frame-skipping decode; with durations = [0, 1] its alignment space strictly contains RNN-T's.\nmonotonic_rnnt_loss — Monotonic RNN-T as TDT with durations = [1] (diagonal-only alignments).\nFastEmit — fastemit_lambda on RNN-T and TDT (Yu et al., arXiv:2010.11148); gradient-only latency regularization.\nMinimum-latency — latency_lambda on RNN-T (Shinohara & Watanabe, Interspeech 2022): augments the loss with the exact expected label emission frame via a first-moment (expectation-semiring) forward-backward.\nhat_loss — Hybrid Autoregressive Transducer (Variani et al. 2020): Bernoulli blank factorization enabling internal-LM subtraction; reuses the shared lattice kernels with its own emissions and gradient.\npruned_rnnt_loss + pruning_bounds — banded lattice (Kuang et al. 2022): the joint lives on O(V·T·S·B) instead of O(V·T·U·B); with a full-width band it equals vanilla RNN-T exactly.\npruned_tdt_loss + tdt_pruning_bounds — the same banding for TDT, including FastEmit (a per-cell gradient reweighting, so banding does not interfere). Bounds are estimated on the TDT lattice, not RNN-T's: frame-skipping blanks move occupancy, and bounds from the wrong transition model silently push alignment mass out of the band.\n\nThey live in one package deliberately: both operate on the same (t frames, u labels) lattice and share the emission gather, α initialization, NLL read-out, label packing, numerics, and test oracles (src/core/). The src/rnnt/ and src/tdt/ subtrees depend only on core/, never on each other — so splitting one out later is trivial, but bundling avoids duplicated kernels or a micro-package depending on another package's internals (the same reason NeMo, k2, and torchaudio ship their transducer variants together).\n\nBoth forward-backwards run as diagonal wavefronts: all lattice cells with t + u = d are independent given neighbouring diagonals, so the host loops over diagonals and each kernel launch fills one anti-diagonal for the whole batch. Every recurrence and gradient formula was validated against brute-force enumeration of all lattice paths and central finite differences (max error ~1e-10) before transcription; the test suite repeats both checks in pure Julia.\n\n\n\n\n\n","title":"TransducerLoss"},{"category":"function","location":"api/#TransducerLoss.rnnt_loss","page":"API","text":"rnnt_loss(logits, targets, input_lengths [; blank, fastemit_lambda, latency_lambda])\nrnnt_loss(logits, targets, input_lengths, blank [; fastemit_lambda, latency_lambda])\nrnnt_loss(logits, target [; input_length, blank, fastemit_lambda, latency_lambda])\n\nRNN-T / transducer loss (Graves 2012), mean over batch.\n\nBatched logits: (V, T, U+1, B) raw joint-network outputs (softmax applied internally), where T bounds encoder frames and U+1 bounds prediction states (⟨start⟩ plus up to U labels). targets::Vector{Vector{Int}}, input_lengths::Vector{Int}.\nSingle-sample logits: (V, T, U+1) with target::Vector{Int}; input_length defaults to T.\nblank defaults to size(logits, 1) (last class), matching CTCLoss.jl.\nlatency_lambda: minimum-latency training (Shinohara & Watanabe, Interspeech 2022) — augments the loss with λ · E[Σᵤ tᵤ] / U, the expected mean label-emission frame under the alignment posterior, computed exactly with a first-moment (expectation-semiring) forward-backward. A genuine objective: finite-difference verified. 0 disables.\nfastemit_lambda: FastEmit regularization (Yu et al., arXiv:2010.11148). Scales label-emission gradients by (1 + λ); 0 disables. The reported loss scalar is unchanged (standard NLL); only gradients are modified, matching torchaudio / NeMo / k2.\n\nGradients are supplied through a ChainRulesCore rrule computed inside the same forward-backward pass, so Zygote never traces the kernels. Memory note: the joint tensor is O(V·T·U·B) — for long inputs train on shorter windows or smaller batches.\n\n\n\n\n\n","title":"TransducerLoss.rnnt_loss"},{"category":"function","location":"api/#TransducerLoss.tdt_loss","page":"API","text":"tdt_loss(logits, dur_logits, targets, input_lengths, durations;\n         blank = size(logits, 1), sigma = 0, fastemit_lambda = 0)\ntdt_loss(logits, dur_logits, target, durations; input_length, blank, sigma,\n         fastemit_lambda)\n\nToken-and-Duration Transducer loss (Xu et al., ICML 2023, arXiv:2304.06795) — the frame-skipping generalization of RNN-T behind NVIDIA's Parakeet-TDT models. Mean over the batch; blank defaults to the last token class (the CTCLoss.jl / TransducerLoss.jl convention).\n\nBatched logits: (V, T, U+1, B), dur_logits: (D, T, U+1, B).\nSingle-sample logits / dur_logits: drop the batch axis; pass one target::Vector{Int} (input_length defaults to T).\ndurations: ascending duration set, e.g. [0, 1, 2, 3, 4] or (0, 1, 2, 3, 4). Blank transitions use durations ≥ 1; tokens may use 0 (multiple tokens per frame, as in RNN-T). The exit blank lands exactly on frame T + 1. Prefer a small NTuple in Flux models so gpu does not turn the table into a CuArray.\nsigma: NeMo's logit under-normalization constant (≈ 0.05 recommended; 0 disables).\nfastemit_lambda: FastEmit regularization; scales label-emission gradients by (1 + λ) on both token and duration heads. Loss scalar unchanged; see rnnt_loss.\n\nWith durations = [0, 1] the alignment space is a strict superset of vanilla RNN-T's (tokens may also advance time directly). With durations = [1] every emission advances time by one frame — equivalent to Monotonic RNN-T (see monotonic_rnnt_loss). Gradients for both logit tensors arrive through a ChainRulesCore rrule computed inside the same forward-backward pass.\n\n\n\n\n\n","title":"TransducerLoss.tdt_loss"},{"category":"function","location":"api/#TransducerLoss.monotonic_rnnt_loss","page":"API","text":"monotonic_rnnt_loss(logits, targets, input_lengths;\n                    blank = size(logits, 1), sigma = 0,\n                    fastemit_lambda = 0)\nmonotonic_rnnt_loss(logits, target; input_length, blank, sigma,\n                    fastemit_lambda)\n\nMonotonic RNN-T loss — at most one label per frame, no vertical token transitions. Implemented as TDT with durations = [1] and a trivial duration head (dur_logits all zeros ⇒ softmax probability 1). Equivalent to restricting the lattice to diagonal label moves plus blank advances. Batched logits are (V, T, U+1, B); single-sample (V, T, U+1).\n\n\n\n\n\n","title":"TransducerLoss.monotonic_rnnt_loss"},{"category":"function","location":"api/#TransducerLoss.hat_loss","page":"API","text":"hat_loss(label_logits, blank_logits, targets, input_lengths)\nhat_loss(label_logits, blank_logits, target; input_length)\n\nHybrid Autoregressive Transducer loss (Variani et al., ICASSP 2020, arXiv:2003.07705). Blank is factorized out as a per-cell Bernoulli: P(blank) = σ(blank_logits[t,u,b]), and labels share (1 − σ) · softmax(label_logits) over a blank-free vocabulary — so batched label_logits is (V, T, U+1, B) with all V classes real labels, and blank_logits is (T, U+1, B). Single-sample forms drop the batch axis.\n\nThe payoff is principled language-model fusion: because the label distribution is a proper conditional LM given \"not blank\", the model's internal LM can be estimated (run the prediction network with the encoder contribution zeroed) and subtracted before adding an external LM — the production-grade alternative to plain shallow fusion. Gradients for both tensors arrive via one ChainRulesCore rrule.\n\n\n\n\n\n","title":"TransducerLoss.hat_loss"},{"category":"function","location":"api/#TransducerLoss.pruned_rnnt_loss","page":"API","text":"pruned_rnnt_loss(logits, band_offsets, targets, input_lengths;\n                 blank = size(logits, 1))\npruned_rnnt_loss(logits, band_offsets, target; input_length, blank)\n\nRNN-T loss on a banded lattice — the memory-critical core of the pruned transducer (Kuang et al., Interspeech 2022, arXiv:2206.13236). The joint is evaluated only on S = size(logits, 3) cells per frame, at lattice positions u = band_offsets[t, b] + s; alignments are confined to the band, so the joint tensor shrinks from O(V·T·U·B) to O(V·T·S·B).\n\nBatched logits are (V, T, S, B) with band_offsets (T, B); single-sample forms are (V, T, S) with a length-T offset vector.\n\nOffsets must be monotone non-decreasing per sample with band_offsets[1, b] == 0; obtain them from pruning_bounds (or any alignment source). With S = U + 1 and zero offsets this is exactly the full RNN-T loss — the test suite asserts that equality.\n\n\n\n\n\n","title":"TransducerLoss.pruned_rnnt_loss"},{"category":"function","location":"api/#TransducerLoss.pruned_tdt_loss","page":"API","text":"pruned_tdt_loss(logits, dur_logits, band_offsets, targets,\n                input_lengths, durations; blank, sigma,\n                fastemit_lambda)\npruned_tdt_loss(logits, dur_logits, band_offsets, target, durations;\n                input_length, blank, sigma, fastemit_lambda)\n\nToken-and-Duration Transducer loss on a banded lattice — pruning (Kuang et al., Interspeech 2022, arXiv:2206.13236) applied to TDT (Xu et al., ICML 2023). The joint is evaluated only on S = size(logits, 3) cells per frame, at lattice positions u = band_offsets[t, b] + s, so both logit tensors shrink from O(T·U·B) to O(T·S·B).\n\nThis is the memory lever for transducer training. Everything else — joint width, batch size, utterance length — is a constant factor on a tensor that is quadratic in the lattice; banding attacks the lattice itself. At a band of 5 against 160 label positions it is a ~32× cut.\n\nBatched: logits (V, T, S, B), dur_logits (D, T, S, B), band_offsets (T, B).\nSingle-sample: drop the batch axis; band_offsets is length T.\ndurations, blank, sigma, fastemit_lambda: as tdt_loss. FastEmit works here — it is a reweighting of the token-departure gradient, which is a per-cell quantity, so banding does not interfere with it.\n\nWith S = U + 1 and zero offsets this is exactly tdt_loss; the test suite asserts that equality on both the loss and both gradients.\n\nAlignments that leave the band are dropped, so the returned value is an upper bound on the true NLL. A band chosen by tdt_pruning_bounds keeps essentially all of the mass; a hand-picked one may not.\n\n\n\n\n\n","title":"TransducerLoss.pruned_tdt_loss"},{"category":"function","location":"api/#TransducerLoss.pack_transducer_targets","page":"API","text":"pack_transducer_targets(targets, blank) → (labels, target_lengths)\n\nPack ragged label sequences into a zero-padded (Umax, B) Int32 matrix plus Vector{Int32} lengths. Targets must not contain blank (the lattice reserves it for the advance-time transition) — throws ArgumentError if they do.\n\n\n\n\n\n","title":"TransducerLoss.pack_transducer_targets"},{"category":"function","location":"api/#TransducerLoss.pruning_bounds","page":"API","text":"pruning_bounds(am, lm, targets, input_lengths; band_width,\n               blank = size(am, 1)) -> Matrix{Int32}\n\nk2-inspired band estimation from the trivial joint am ⊕ lm (encoder logits (V, T, B), prediction logits (V, U+1, B)), which never materializes a (V, T, U+1, B) tensor: per-cell normalizers are reduced inside a kernel, the shared lattice forward-backward runs on the resulting (T, U+1, B) emissions, and the band is centred on the label-posterior mass per frame (monotone, start-anchored, exit-reachable).\n\nSimplification stated honestly: k2 derives bounds from the gradients of a smoothed trivial-joint loss; centring on posterior mass is a simpler proxy that keeps the same guarantees (monotone, feasible) but is not bit-compatible with k2. Throws if band_width is too small to sweep from the start cell to the exit within T frames.\n\n\n\n\n\n","title":"TransducerLoss.pruning_bounds"},{"category":"function","location":"api/#TransducerLoss.tdt_pruning_bounds","page":"API","text":"tdt_pruning_bounds(am, lm, am_dur, lm_dur, targets, input_lengths;\n                   band_width, durations, blank = size(am, 1), sigma = 0)\n    -> Matrix{Int32}\n\nDuration-aware band estimation from the trivial joint: token logits am (V, T, B) ⊕ lm (V, U+1, B) and duration logits am_dur (D, T, B) ⊕ lm_dur (D, U+1, B). The (V, T, U+1, B) token tensor never materializes — per-cell normalizers are reduced inside a kernel — and the duration tensor that does materialize is (D, T, U+1, B), smaller by V/D.\n\nUses the TDT lattice, not RNN-T's. That matters: a duration-d blank skips d frames without advancing u, so TDT occupancy sits lower and flatter in u than RNN-T's at the same frame. Bounds estimated with the wrong transition model push real alignment mass outside the band, and the loss silently rises rather than failing.\n\nCentres each frame's band on cell occupancy α + β − logZ rather than on RNN-T pruning_bounds' label-emission posterior. Occupancy is the probability of passing through (t, u) at all, which is the quantity the band is supposed to contain, and it needs no duration-weighted sum over exit transitions. Offsets are then made monotone, capped at band_width − 1 growth per frame so a path can still climb inside the band, and ramped so the exit stays reachable.\n\nThrows if band_width is too small to sweep from the start cell to the exit within T frames.\n\n\n\n\n\n","title":"TransducerLoss.tdt_pruning_bounds"},{"category":"function","location":"api/#TransducerLoss.rnnt_forward_backward","page":"API","text":"rnnt_forward_backward(logits, labels, target_lengths, input_lengths,\n                      blank, fastemit_lambda)\n\nInternal: run the transducer forward-backward on whatever device holds logits and return (loss, grad) where loss is the batch-mean NLL and grad is ∂loss/∂logits (same shape as logits, already scaled by 1/B). Use pack_transducer_targets to build labels and target_lengths.\n\n\n\n\n\n","title":"TransducerLoss.rnnt_forward_backward"},{"category":"function","location":"api/#TransducerLoss.tdt_forward_backward","page":"API","text":"tdt_forward_backward(logits, dur_logits, labels, target_lengths,\n                     input_lengths, durations, blank, sigma)\n\nInternal: run the TDT forward-backward on whatever device holds logits and return (loss, grad_tok, grad_dur) — the batch-mean NLL and the gradients w.r.t. both raw-logit tensors (already scaled by 1/B).\n\n\n\n\n\n","title":"TransducerLoss.tdt_forward_backward"},{"category":"function","location":"api/#TransducerLoss.hat_forward_backward","page":"API","text":"hat_forward_backward(label_logits, blank_logits, labels, target_lengths,\n                     input_lengths)\n\nInternal: (loss, grad_labels, grad_blank) — batch-mean HAT NLL and gradients w.r.t. both raw-logit tensors (scaled by 1/B).\n\n\n\n\n\n","title":"TransducerLoss.hat_forward_backward"},{"category":"function","location":"api/#TransducerLoss.pruned_forward_backward","page":"API","text":"pruned_forward_backward(logits, band_offsets, labels, target_lengths,\n                        input_lengths, blank)\n\nInternal: (loss, grad) for the banded lattice. logits is (V, T, S, B) — the joint evaluated only on band cells — and band_offsets maps band slot s at frame t to lattice cell u = band_offsets[t, b] + s.\n\n\n\n\n\n","title":"TransducerLoss.pruned_forward_backward"},{"category":"function","location":"api/#TransducerLoss.logaddexp","page":"API","text":"logaddexp(a, b)\n\nNumerically stable log(exp(a) + exp(b)). Internal; qualify as TransducerLoss.logaddexp if needed.\n\n\n\n\n\n","title":"TransducerLoss.logaddexp"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"Minimal calls for every exported loss. Shapes follow Choosing a loss. Replace logits, targets, and input_lengths with your model outputs.","title":"Examples"},{"category":"section","location":"examples/#RNN-T","page":"Examples","text":"using TransducerLoss\n\n# logits: (V, T, U+1, B) — raw joint-network output\nloss = rnnt_loss(logits, targets, input_lengths)\nloss = rnnt_loss(logits, targets, input_lengths; blank = V)","title":"RNN-T"},{"category":"section","location":"examples/#TDT","page":"Examples","text":"# logits (V, T, U+1, B), dur_logits (D, T, U+1, B)\ndurations = [0, 1, 2, 3, 4]\nloss = tdt_loss(logits, dur_logits, targets, input_lengths, durations;\n                        sigma = 0.05)","title":"TDT"},{"category":"section","location":"examples/#Monotonic-RNN-T","page":"Examples","text":"# logits (V, T, U+1, B) — no duration head needed\nloss = monotonic_rnnt_loss(logits, targets, input_lengths)","title":"Monotonic RNN-T"},{"category":"section","location":"examples/#HAT","page":"Examples","text":"# label_logits (V, T, U+1, B) blank-free; blank_logits (T, U+1, B)\nloss = hat_loss(label_logits, blank_logits, targets, input_lengths)","title":"HAT"},{"category":"section","location":"examples/#Pruned-RNN-T","page":"Examples","text":"# Step 1: estimate band from encoder (am) and predictor (lm) logits\noffsets = pruning_bounds(am, lm, targets, input_lengths; band_width = 4)\n\n# Step 2: loss on banded joint logits (V, T, S, B)\nloss = pruned_rnnt_loss(pruned_logits, offsets, targets, input_lengths)\n\npruning_bounds alone — useful when you only need offsets before gathering pruned logits in your model code:\n\noffsets = pruning_bounds(am, lm, targets, input_lengths; band_width = 4)\n# offsets: (T, B) Int matrix; cell u = offsets[t, b] + s for s in 1:S","title":"Pruned RNN-T"},{"category":"section","location":"examples/#Pruned-TDT","page":"Examples","text":"# Bounds from the TDT lattice (occupancy), not RNN-T's\noffsets = tdt_pruning_bounds(am, lm, am_dur, lm_dur, targets, input_lengths;\n                             band_width = 4, durations)\n\n# Banded token (V, T, S, B) + duration (D, T, S, B) logits\nloss = pruned_tdt_loss(pruned_logits, pruned_dur_logits, offsets,\n                               targets, input_lengths, durations;\n                               sigma = 0.05)","title":"Pruned TDT"},{"category":"section","location":"examples/#Regularization","page":"Examples","text":"# FastEmit — gradient-only; works on RNN-T and TDT\nloss = rnnt_loss(logits, targets, input_lengths; fastemit_lambda = 0.004)\nloss = tdt_loss(logits, dur_logits, targets, input_lengths, durations;\n                        fastemit_lambda = 0.004)\n\n# Minimum-latency — RNN-T only; augments the loss scalar\nloss = rnnt_loss(logits, targets, input_lengths; latency_lambda = 0.01)","title":"Regularization"},{"category":"section","location":"examples/#Zygote-gradients","page":"Examples","text":"using TransducerLoss, Zygote\n\ngrad = Zygote.gradient(\n    l -> rnnt_loss(l, targets, input_lengths),\n    logits,\n)[1]\n\ngtok, gdur = Zygote.gradient(\n    (l, d) -> tdt_loss(l, d, targets, input_lengths, durations),\n    logits, dur_logits,\n)","title":"Zygote gradients"},{"category":"section","location":"#TransducerLoss.jl","page":"Home","text":"Batched transducer-loss functions for Julia — GPU-capable via KernelAbstractions, differentiable via ChainRulesCore (Zygote-compatible). Companion to CTCLoss.jl: blank defaults to the last class, loss is the batch mean, gradients w.r.t. raw logits are computed inside one forward-backward per call.","title":"TransducerLoss.jl"},{"category":"section","location":"#Losses,-not-model-heads","page":"Home","text":"This package exports training objectives — functions that take logits your model already produced and return a scalar loss (plus analytic gradients via rrules). It does not define encoder, predictor, or joint-network layers.\n\nIn a typical ASR model, the joint network outputs one or more logit tensors. You pass those tensors here:\n\nYour model produces You call\nOne joint logit tensor (V, T, U+1, B) rnnt_loss\nToken head + duration head tdt_loss\nLabel head + blank head hat_loss\nBanded joint (V, T, S, B) + offsets pruned_rnnt_loss\nBanded token + duration heads + offsets pruned_tdt_loss\n\nTDT and HAT correspond to architectures with multiple output heads, but TransducerLoss only implements the loss computation on their logits — not the heads themselves.","title":"Losses, not model heads"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url = \"https://github.com/mashu/TransducerLoss.jl\")\n\nFrom a local checkout:\n\nPkg.develop(path = \"/path/to/TransducerLoss.jl\")","title":"Installation"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Page Purpose\nChoosing a loss Decision table — variant, shapes, when to use, why it exists\nExamples One minimal snippet per exported loss\nAPI Full docstrings and internal forward-backward entry points","title":"Where to go next"},{"category":"section","location":"#Shared-conventions","page":"Home","text":"All variants share these inputs (unless noted otherwise):\n\ntargets: Vector{Vector{Int}} — label indices per utterance (no blank).\ninput_lengths: Vector{Int} — valid encoder frames per sample (≤ T).\nblank: defaults to size(logits, 1) (last class), matching CTCLoss.jl.\n\nLattice axes: T bounds encoder frames; U+1 bounds prediction states (⟨start⟩ plus up to U target labels). Logits are raw (softmax / sigmoid applied inside the loss).","title":"Shared conventions"},{"category":"section","location":"#License","page":"Home","text":"MIT.","title":"License"}]
}
